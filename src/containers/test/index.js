import React from 'react';
import { View, Text } from 'ui';

const TestContainer = () => {
    return (
        <View
            style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}
        >
            <Text>This is a test container</Text>
        </View>
    );
};

export default TestContainer;
